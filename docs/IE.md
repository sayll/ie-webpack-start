# IE

## 特别说明
此部分介绍如何将打包的代码尽量兼容到IE8以下。但由于各种局限原因，可能存在许多坑。请用户在非私人项目中谨慎使用，有此引发的后果，作者一律不会承担。

## 项目中的注意事项
1. 所有引用包必须支持低版本IE，且引用的入口只有一处。 [`/app/static/index.js`](/app/) 【入口做了特殊处理】

2. 只能使用`require`引包,而无法使用import 和 export;

3. 